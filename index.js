const add = (x, y) => x + y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

const firstInput = '';
const operatorBtn = '';
const secondInput = '';

const operate = (x, y) => {
    return add(x, y);
};


console.log(operate(1, 2));